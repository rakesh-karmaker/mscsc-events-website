export type SegmentType = {
  icon: string;
  imageUrl: string;
  title: string;
  summary: string;
  details: string;
};

export type ScheduleType = {
  icon: string;
  time: string;
  title: string;
  description: string;
};

export type SponsorType = {
  name: string;
  logoUrl: string;
  websiteUrl: string;
};

export type FaqType = {
  question: string;
  answer: string;
};
